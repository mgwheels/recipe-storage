name: Python Tests via Makefile

on: [push, pull_request]

jobs:
  linting:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/setup
      - run: make lint

  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/setup
      - run: make test