name: 'Standard Setup'
description: 'Checkout and setup Python'
runs:
  using: "composite"
  steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'
    - run: make deps
      shell: bash
